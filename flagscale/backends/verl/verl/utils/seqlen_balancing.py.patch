diff --git a/verl/utils/seqlen_balancing.py b/verl/utils/seqlen_balancing.py
index c27b0e39..905f6ca4 100644
--- a/verl/utils/seqlen_balancing.py
+++ b/verl/utils/seqlen_balancing.py
@@ -13,6 +13,7 @@
 # limitations under the License.
 
 import copy
+import enum
 import heapq
 import math
 import random
@@ -30,6 +31,7 @@ from verl.utils.device import get_device_name
 class PackingAlgorithm(enum.Enum):
     """Enum for supported sequence packing algorithms."""
 
+    KARMARKAR_KARP = "karmarkar_karp"
     CONCATENATIVE = "concatenative"
     FIRST_FIT_DECREASING = "first_fit_decreasing"
     FIRST_FIT_SHUFFLE = "first_fit_shuffle"
@@ -869,7 +871,6 @@ def get_advanced_seqlen_balanced_partitions(
     """
     if sequence_packing_config is not None:
         packing_algorithm = sequence_packing_config.get("algorithm", packing_algorithm)
-        bin_capacity = sequence_packing_config.get("max_tokens_per_microbatch", bin_capacity)
     
     if packing_algorithm == PackingAlgorithm.KARMARKAR_KARP:
         return get_seqlen_balanced_partitions(seqlen_list, k_partitions, equal_size)
